Wave to mp3 converter

Platform: Linux
Dependencies: LAME

Build:
Requires gcc for compilation
Requires installed lame library ( which can be done by typeing "sudo apt-get install libmp3lame-dev" in terminal )
Building is done with makefile, get into directory of the project folder and type "make" in console

Options:
Program requires 2 parameters to run
-i input wav file
-o name of the outupt mp3, for example "result.mp3"